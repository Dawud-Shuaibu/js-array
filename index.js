/* JavaScript Reference Types

1. Array.
*/

// Array
let selectedColor = ['black', 'green'];

// Changing a value of a data in Array
selectedColor['0'] = 'red';

// Adding a data in Array
selectedColor['2'] = 'blue';

console.log(selectedColor);

// Logging one data in the console
console.log(selectedColor['2']);